[package]
name = "agent-terminal-e2e"
version = "0.1.0"
edition = "2021"

[dependencies]
agent-terminal-core = { path = "../core" }
tokio = { version = "1", features = ["full"] }
anyhow = "1"
portable-pty = "0.8"
base64 = "0.22"
uuid = { version = "1", features = ["v4"] }
